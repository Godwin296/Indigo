# Copie ce fichier en `.env` (jamais commité) et renseigne tes vraies valeurs.
# Trouvable dans ton projet Supabase : Settings > API.

EXPO_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ⚠️ Ne jamais mettre la clé "service_role" ici (elle contourne toutes les
# règles RLS) — celle-là reste uniquement côté serveur (Edge Functions).
